<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/layout_toolbar"
        layout="@layout/toolbar" />

    <com.xgame.ui.activity.personal.view.RefreshLayout
        android:id="@+id/refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/bottom_btn"
        android:layout_below="@+id/layout_toolbar"
        app:container="@layout/layout_user_detail" />

    <com.xgame.ui.activity.invite.view.RelationButton
        android:id="@+id/bottom_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/large_purple_round_btn"
        android:gravity="center_horizontal"
        android:paddingTop="@dimen/dp_15"
        app:textBold="true"
        app:textColor="@color/color_white"
        app:textGravityCenter="false"
        app:textSize="@dimen/sp_16" />
</RelativeLayout>
